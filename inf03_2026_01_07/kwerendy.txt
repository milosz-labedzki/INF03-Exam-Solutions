SELECT AVG(temperatura) FROM `pomiary` WHERE id_miesiac = 7;

INSERT INTO `miejscowosc`(`nazwa`, `kraj`) VALUES ('Kijów','Ukraina');

SELECT nazwa,kraj,temperatura FROM `miejscowosc` JOIN pomiary ON miejscowosc.id= pomiary.id_miejscowosc WHERE id_miesiac=7;

SELECT nazwa,AVG(temperatura) FROM `miesiace` JOIN pomiary ON miesiace.id=pomiary.id_miesiac GROUP BY nazwa;